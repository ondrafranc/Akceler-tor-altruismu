{
  "framework": null,
  "buildCommand": "cd akcelerator-landing-page && npm ci && npm run build && cd .. && mkdir -p .vercel && cp -r akcelerator-landing-page/.vercel/output .vercel/",
  "installCommand": "echo 'Install step skipped - handled in build'"
} 